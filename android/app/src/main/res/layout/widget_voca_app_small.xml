<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:background="@drawable/widget_background_blue">

    <!-- Header -->
    <TextView
        android:id="@+id/widget_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="Voca"
        android:textColor="#B3FFFFFF"
        android:textSize="14sp" />

    <!-- Total Words -->
    <LinearLayout
        android:id="@+id/widget_stats_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom">

            <TextView
                android:id="@+id/widget_total_words"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textColor="#FFFFFF"
                android:textSize="36sp"
                android:fontFamily="sans-serif-medium" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=" words"
                android:textColor="#B3FFFFFF"
                android:textSize="14sp"
                android:layout_marginBottom="8dp" />
        </LinearLayout>

        <!-- Today Words -->
        <TextView
            android:id="@+id/widget_today_words"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+0 today"
            android:textColor="#4ADE80"
            android:textSize="13sp"
            android:visibility="gone" />

        <!-- Study Sessions -->
        <TextView
            android:id="@+id/widget_study_sessions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 study sessions"
            android:textColor="#60A5FA"
            android:textSize="13sp"
            android:visibility="gone" />
    </LinearLayout>

</RelativeLayout>
