// API configuration
// WebView mode: All requests use relative paths (same origin as the loaded web page)

export function getApiUrl(): string {
  return '';
}

export function getApiEndpoint(path: string): string {
  return path;
}
